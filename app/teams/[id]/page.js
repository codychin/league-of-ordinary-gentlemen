import Link from 'next/link';import {teams} from '../data';import SiteNav from '../../components/SiteNav'
export default async function Team({params}){const {id}=await params;const t=teams.find(x=>x.id===id);if(!t)return <main>Team not found.</main>;return <><header className="articleHeader"><Link href="/" className="miniMast">The Brief of Ordinary Gentleman</Link><SiteNav/></header><main className="teamPage"><div className="eyebrow">FRANCHISE FILE • 2026</div><h1>{t.team}</h1><div className="ownerLine"><b>{t.owners}</b><span>{t.aliases}</span></div><div className="teamStats four"><div><b>{t.record}</b><span>RECORD</span></div><div><b>{t.pf}</b><span>PF</span></div><div><b>{t.pa}</b><span>PA</span></div><div><b>W1</b><span>THROUGH</span></div></div><section className="franchiseLead"><div><small>WEEK 1</small><h2>{t.week1}</h2><p>{t.note}</p></div><aside><small>ON FILE</small><p>{t.lore}</p></aside></section><div className="hubGrid"><section><div className="hubHead">WEEK 1 / LEADING RETURNS</div>{t.stars.map((x,i)=><div className="ledgerRow" key={i}><b>{String(i+1).padStart(2,'0')}</b><span>{x}</span></div>)}</section><section><div className="hubHead">TRANSACTION DESK</div>{t.moves.length?t.moves.map((x,i)=><div className="ledgerRow" key={i}><b>→</b><span>{x}</span></div>):<p className="quiet">No material Week 2 activity on file.</p>}</section></div><section className="mythology"><div className="hubHead">FRANCHISE MYTHOLOGY</div><div className="mythGrid">{t.mythology.map((x,i)=>{const [head,...rest]=x.split(' — ');return <article key={i}><small>FILE {String(i+1).padStart(2,'0')}</small><h3>{head}</h3><p>{rest.join(' — ')}</p></article>})}</div></section><section className="coverage"><div className="hubHead">RECENT COVERAGE</div>{t.coverage.length?t.coverage.map(([slug,title])=><Link href={`/articles/${slug}`} key={slug}><span>THE BRIEF</span><b>{title}</b><em>READ →</em></Link>):<p className="quiet">No dedicated coverage yet. This should not be interpreted as institutional approval.</p>}</section><div className="teamFoot"><span>Team names change. The file does not.</span><Link href="/teams" className="back">← ALL FRANCHISES</Link></div></main></>}
