import Link from 'next/link';import {teams} from './data';import SiteNav from '../components/SiteNav'
export default function Teams(){return <><header className="articleHeader"><Link href="/" className="miniMast">The Brief of Ordinary Gentleman</Link><SiteNav/></header><main><section className="directoryHead"><div className="eyebrow">THE DIRECTORY • PERSONNEL FILES</div><h1>Franchise Dossiers</h1><p>Twelve teams. Twenty-seven aliases. One season of evidence already being wildly overinterpreted.</p></section><div className="directory">{teams.map(t=><Link href={`/teams/${t.id}`} key={t.id}><div className="record">{t.record}</div><small>{t.owners}</small><h2>{t.team}</h2><div className="aliases">{t.aliases}</div><p>{t.note}</p><span>OPEN THE FILE →</span></Link>)}</div></main></>}
