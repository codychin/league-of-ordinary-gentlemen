import Link from 'next/link'
import {archive} from '../articles/data'
import SiteNav from '../components/SiteNav'
export default function Archive(){const weeks=[...new Set(archive.map(a=>a.week))].sort((a,b)=>b-a);return <><header className="articleHeader"><Link href="/" className="miniMast">The Brief of Ordinary Gentleman</Link><SiteNav/></header><main><section className="directoryHead"><div className="eyebrow">THE PERMANENT RECORD</div><h1>Article Archive</h1><p>Every take preserved long enough to become evidence.</p></section><div className="archiveWrap">{weeks.map(w=><section className="archiveWeek" key={w}><div className="weekNum">WEEK {w}</div><div>{archive.filter(a=>a.week===w).map(a=><Link href={`/articles/${a.slug}`} className="archiveStory" key={a.slug}><small>{a.section}</small><h2>{a.title}</h2><p>{a.dek}</p><span>READ / SAVE FOR RECEIPTS →</span></Link>)}</div></section>)}</div></main></>}
